import React from 'react';
import {Box, Text} from 'native-base';

interface ICartMainScreenProps {}

const MainScreen: React.FC<ICartMainScreenProps> = props => {
  return (
    <Box>
      <Text>Cart Screen</Text>
    </Box>
  );
};

export default MainScreen;
